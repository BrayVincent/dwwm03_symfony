{% extends 'base.html.twig' %}

{% block title %}Update account
{% endblock %}

{% block body %}
	<div class="container ">
		<h2 class=" my-5">Update Profile</h2>
		{{ form_start(form) }}
		{{ form_widget(form) }}
		{{ form_end(form) }}
	</div>
	<a href="{{ path('app_login') }}" class="btn btn-primary btn-sm mt-5">Revenir à l'arrière</a>
{% endblock %}
